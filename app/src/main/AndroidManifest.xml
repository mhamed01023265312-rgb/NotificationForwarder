<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- الإذونات الأساسية -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- 🌟 إذن التسجيل الصوتي للمساعد البكسلي -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <!-- 🌟 إذونات تشغيل الخدمات في الخلفية بانتظام (Android 13+ & 14+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".NotificationForwarderApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Pixel-Boy AI"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotificationForwarder">

        <!-- 🌟 تم تعديل enabled إلى true ليتمكن التطبيق من الفتح للمرة الأولى بشكل طبيعي -->
        <activity
            android:name=".MainActivity"
            android:enabled="true"
            android:exported="true"
            android:launchMode="singleTask">
        </activity>

        <!-- 🌟 المدخل الوحيد للأيقونة على الشاشة الرئيسية عند التثبيت -->
        <activity-alias
            android:name=".LauncherAlias"
            android:targetActivity=".MainActivity"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="Pixel-Boy AI">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- 🌟 خدمة قراءة الإشعارات مع إضافة نوع الخدمة للتوافق مع أندرويد 14 -->
        <service
            android:name=".service.AppNotificationListenerService"
            android:exported="true"
            android:foregroundServiceType="dataSync"
            android:label="Notification Forwarder"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- مستقبل التشغيل التلقائي عند إعادة تشغيل الهاتف -->
        <receiver
            android:name=".receiver.BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
